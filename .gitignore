# Godot build cache and imports
.godot/
__pycache__/
# Retained source caches used by the offline asset tools
tools/downloads/
# Movie Maker output and encodes
*.avi
*.mp4
# Editor leftovers
*.tmp
.DS_Store

# rendered videos (project output stays in the repo, ignored)
renders/
/local-data/
/.worktrees/
/.env
/.env.*
/override.cfg
*.log
*.x86_64
*.zip
*.tar.gz

# .NET SDK output (Godot normally places these under .godot/)
/bin/
/obj/
